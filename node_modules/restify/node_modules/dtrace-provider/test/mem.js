// node --expose_gc ...

var d = require('../dtrace-provider');

var dtp = d.createDTraceProvider("memtest" + i);
var p = dtp.addProbe("gcprobe", "int");
dtp.enable();

for (var i = 0; i < 10000000; i++) {
    p.fire(function () {
        return [1];
    });
}
