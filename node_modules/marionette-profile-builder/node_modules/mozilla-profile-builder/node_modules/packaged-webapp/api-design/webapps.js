// this module is responsible for private manipulation
// of the webapps.json file.

// all operations are done on an existing object.
var json = {};

webapps.nextLocalId(); // xxx

webapps.add(hostname, {
  origin: origin
});
