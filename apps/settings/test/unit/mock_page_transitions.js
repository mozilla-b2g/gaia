/* global define */
define(function() {
  'use strict';
  return {
    oneColumn: function(oldPanel, newPanel, callback) {
      setTimeout(callback);
    },
    twoColumn: function(oldPanel, newPanel, callback) {
      setTimeout(callback);
    }
  };
});
