The the original dictionary file - data/rawdict are from android open source
project. It is part of the PinyinIME project of the android.
http://android.googlesource.com/platform/packages/inputmethods/PinyinIME

Database JSON files:

1. Install SpiderMonkey JavaScript Shell(http://ftp.mozilla.org/pub/mozilla.org/firefox/nightly/latest-trunk/)

2. Run the following commands to create the simplifed Chinese database JSON files:
   $ cd data
   $ make

./db.json will be updated.

3. Use the Microsoft Office Word or other tool to convert the db.json to the
traditional Chinese file and save it as
./db-tr.json.

Running tests:

1. Install SpiderMonkey JavaScript Shell(http://ftp.mozilla.org/pub/mozilla.org/firefox/nightly/latest-trunk/)

2. Run the tests:
   $ cd tests
   $ make

3. Clean up the temporary files of the tests:
   $ cd tests
   $ make clean
