/**
 * Alternate-named shim for 'axe-logger', referenced in third-party
 * dependencies. This could alternately just be included within our
 * RequireJS configuration.
 */
define(['axe-logger'], function(axe) {
  return axe;
});
